import { useEffect } from "react";
import { GetAllMovies } from "../apicalls/movies";

const Home = () => {
  // const [movie, setMovies] = usState([]);
  // const getData = async () => {
  //   try {
  //     const response = await GetAllMovies();
  //     setMovies(response.data.movies);
  //   } catch (error) {}
  // };
  // useEffect(() => {}, []);
  // return;
  return <div>HOME PAGE</div>;
};

export default Home;
